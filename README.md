# Breast Cancer Detection

A machine learning project to detect and classify breast cancer using a dataset of clinical features.

## Project Structure
* `data.csv` - The dataset containing patient clinical features and diagnostic labels.
* `main.py` - (Or your notebook/script name) Data preprocessing, model training, and evaluation.

## Getting Started

### Prerequisites
You will need Python 3 and the following data science libraries:
```bash
pip install numpy pandas scikit-learn matplotlib seaborn
